import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color.fromRGBO(217, 189, 50, 100);
  static const secondary = Color.fromRGBO(249, 231, 50, 100);

  static const background = Color.fromRGBO(242, 211, 53, 100);
  static const surface = Colors.white;

  static const textPrimary = Color(0xFF212121);
  static const textSecondary = Color(0xFF757575);
  static const error = Color(0xFFD32F2F);

  static const buttonThemeColor = Color.fromRGBO(249, 231, 50, 100);
  static const buttonSecondary = Colors.white;
  static const buttonBorderColor = Color(0xFF212121);
}
